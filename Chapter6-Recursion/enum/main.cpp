#include <iostream>
#include <iomanip>
#include <cmath>
#include <ctime>

#include "enum.h"

/*
    enum Status {CONTINUE, WON, LOST}; //creates an enumeration named status with specific parameters
    Status gameStatus; //creates an enumerate object called gameStatus

    if(...)
        gameStatus = WON;

    enum assigns values to conditions from 0 to n. 

    Another way of using enum:
    enum Months {JAN = 1, FEB, MAR, APR, 
                MAY, JUN, JUL, AUG,
                SEP, OCT, NOV, DEC };

    Notice that you can set the starting value
*/

int main(){
    Enum k;
    k.enumerate();

}